# Phase 16: ドキュメントと品質保証 (Documentation & QA)

## 概要
機能追加やUI刷新に伴い陳腐化したユーザーガイドを全面的に刷新し、最新の状態にします。また、プロジェクトの信頼性を担保するため、不足しているテストコードを拡充します。

## タスク一覧

### 1. ドキュメント刷新
- [x] **User Guideの全面改訂**
    - **内容**: `docs/user_guide.md` を最新のv1.1.0以降の仕様（NavigationSplitView、Liquid Glass UI、新機能）に合わせて書き直す。
    - **スクリーンショット**: 以下の主要画面のスクリーンショットを撮り直し、差し替える。
        - アプリ概要 (Overview)
        - インポート・プロジェクト管理画面
        - 波形編集・再生画面
        - インスペクタ・詳細画面
        - エクスポートメニュー

### 2. テスト拡充
- [ ] **テストカバレッジの向上**
    - **内容**: 既存の `RehearsalLinkTests` を見直し、テストが不足しているモジュールやエッジケースに対するテストケースを追加する。
    - **重点領域**:
        - `MainViewModel` の複雑な状態遷移
        - 新規追加される「演奏セグメントの文字起こし」ロジック
        - エクスポート機能のサービス層 (`ExportService`)
